<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:ffimageloading="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
             x:Class="Martivi.Pages.ProfilePage" Title="პროფილი" BindingContext="{StaticResource MainViewModel}">
    <ContentPage.Content>
        <Frame Margin="4" HasShadow="True">
            <Grid >
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <StackLayout>
                <ffimageloading:CachedImage HorizontalOptions="Center"  ErrorPlaceholder="blankprofile.png" Source="{Binding CurrentUser.ProfileImageUrl, TargetNullValue=blankprofile.png}" HeightRequest="200" DownsampleToViewSize="true" Aspect="Fill" CacheType="All"></ffimageloading:CachedImage>
                <Label HorizontalOptions="Center" Text="{Binding UserName}"></Label>
            </StackLayout>
        </Grid>
        </Frame>
        
    </ContentPage.Content>
</ContentPage>